# Runtime Contract

Generated by `piphi-network-create`.

- Integration ID: `piphi-network-zigbee`
- Kind: `integration`
- Preset: `protocol-bridge`
- Domain: `bridge`
- Port: `8730`
- Required service: `piphi.service.zigbee2mqtt-sidecar`

## Required Endpoints

- `GET /health`
- `GET /entities`
- `POST /command`
- `POST /config`
- `GET /ui-config`

## Automation Command Payload

`POST /command` accepts the Core automation runtime contract:

```json
{
  "contract_version": "automation.runtime.command.v1",
  "command": "refresh",
  "target": {
    "config_id": "demo-device",
    "device_id": "demo-device",
    "entity_id": null
  },
  "params": {},
  "capability": "device.refresh",
  "capability_requirements": ["device.refresh"]
}
```

Return `{"ok": true, ...}` for accepted commands. For validation failures, return `{"ok": false, "error": "unsupported_capability", "message": "..."}` with an appropriate 4xx status.

Supported commands are `refresh`, `set_state`, `set_brightness`, and
`set_color_temperature`. The current implementation records accepted command
events; MQTT publish support is the next runtime phase.

## Recommended Endpoints

- `GET /diagnostics`
- `POST /discover`
- `POST /config/sync`
- `POST /deconfigure`
- `GET /state`
- `GET /contract`
- `GET /events`
- `POST /telemetry/example`

Keep `manifest.json`, the contract source file, and route handlers in sync.
